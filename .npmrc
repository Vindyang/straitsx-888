# Allow deterministic non-interactive installs to replace generated node_modules
# when pnpm detects a different runtime layout. This does not change the
# onlyBuiltDependencies supply-chain allowlist in package.json.
confirm-modules-purge=false
